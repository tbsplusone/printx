# printx

A printf minimal clone.

```bash
Usage: 
  printx [OPTIONS] FORMAT [ARGUMENT]...
  
OPTIONS:
  -h, --help         print this help
  -v, --version      print version
  -a, --assing VAR   assign result to variable VAR

FORMAT sequences:
  %s   ARGUMENT is printed as string
  %%   a single %
```
